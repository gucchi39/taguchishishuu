/* ========================================
   営業カレンダー設定ファイル
   ----------------------------------------
   休業日が決まったら、このファイルを書き換えて
   commit → push すると、サイトのカレンダーに反映されます。
   ======================================== */

/**
 * カレンダーの表示方法
 *
 *   'builtin' … このサイトに内蔵のカレンダー（下の休業日データを使用）
 *   'google'  … Googleカレンダーを埋め込む（googleCalendarId の設定が必要）
 *
 * ※ まずは 'builtin' のまま運用し、Googleカレンダーの準備ができたら
 *    'google' に書き換えてください。
 * ※ 'google' 以外の値を書いた場合は、安全のため 'builtin' として動作します。
 */
export const calendarMode: 'builtin' | 'google' = 'builtin'

/**
 * Googleカレンダーを使う場合のカレンダーID
 *
 * 【重要】必ず「営業カレンダー専用」のカレンダーを新規作成してください。
 *         普段お使いの個人カレンダーを公開すると、私用の予定まで
 *         誰でも見られる状態になってしまいます。
 *
 * 【手順】
 *   1. Googleカレンダーを開く
 *   2. 左側「他のカレンダー」の「＋」→「新しいカレンダーを作成」
 *      名前は「営業カレンダー」など分かりやすいものに
 *   3. 作成したカレンダーの「設定と共有」を開く
 *      （個人カレンダーではなく、いま作ったカレンダーであることを必ず確認）
 *   4. 「予定のアクセス権限」→「一般公開して誰でも利用できるようにする」にチェック
 *   5. 「カレンダーの統合」→「カレンダーID」をコピーして下に貼り付け
 *
 * 例: 'example@group.calendar.google.com'
 */
export const googleCalendarId = ''

/**
 * 毎週の定休日
 * 0=日曜, 1=月曜, 2=火曜, 3=水曜, 4=木曜, 5=金曜, 6=土曜
 *
 * 現在はサイト内の記載（日曜・祭日・土曜不定期休）にあわせて日曜のみ設定しています。
 */
export const weeklyClosedDays: number[] = [0]

/**
 * 個別の休業日（祝日・お盆・年末年始・臨時休業など）
 *
 * 書き方は 'YYYY-MM-DD' が基本ですが、
 * '2026/1/1' のようなスラッシュ区切りやゼロ埋めなしでも認識します。
 *
 * 例:
 *   '2027-01-01',   // 元日
 *   '2026-08-13',   // お盆休み
 *
 * ※ ここが空のあいだは、カレンダー上に
 *   「祝日・お盆・年末年始は準備中」の案内が自動で表示されます。
 */
export const closedDates: string[] = [
  // ここに休業日を追加してください
]

/**
 * 臨時営業日（定休日だけれど営業する日）
 * こちらに書いた日は、上の定休日設定よりも優先して「営業日」になります。
 *
 * 例:
 *   '2026-05-10',   // 日曜だが特別営業
 */
export const openDates: string[] = [
  // ここに臨時営業日を追加してください
]

/** カレンダーの下に表示する補足メッセージ（不要なら空文字に） */
export const calendarNote =
  '土曜日は不定期でお休みをいただく場合がございます。お急ぎのご用命は事前にお電話にてご確認ください。'

/**
 * closedDates が空のときにカレンダー上へ表示する、お客様向けの案内文。
 * 祝日などが未登録のまま「営業日」と誤解されるのを防ぐためのものです。
 */
export const pendingHolidayNote =
  '祝日・お盆・年末年始などの休業日は現在準備中です。該当時期のご用命は、お手数ですがお電話にてご確認ください。'
